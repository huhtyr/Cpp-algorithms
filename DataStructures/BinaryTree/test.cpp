#include <iostream>
#include <vector>
using namespace std;
int main(){
    vector<int> v(101, 1);
    //a+b = 4*m + 3
    int n, count = 0;
    cin >> n;
    for(int m = 0; 4*m+3 < n; m++){
        //3, 7, 11, 15...
        
    }

    return 0;
}